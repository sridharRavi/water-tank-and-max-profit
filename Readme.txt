* Download the repo into a single folder
* Make sure the css, JS and html file are in the same folder
* Run the html file (double click it) to opn it in the browser
* The problem with the solution will appear in the html page. Change the input field for dynamic generation of the
SVG solution
* Use the python file for the Max Profit problem
* Run with command python Max_profit.py . Check the python version before using the command
I am running it on python version Python 3.11.4 